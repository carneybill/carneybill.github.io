if (file.exists('~/.Rprofile')) sys.source('~/.Rprofile')

options(
  digits = 4, servr.daemon = TRUE, formatR.indent = 2,
  blogdown.publishDir = '../rstudio-blog-public', blogdown.yaml.empty = FALSE
)
